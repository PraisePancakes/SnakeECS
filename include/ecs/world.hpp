#pragma once
#include "entity.hpp"

namespace snek
{
    class World
    {
        //add entity
        //remove entity
        //add_entity(tag)
        //get_entity(id)
        //has_entity(id)
        //get_entities(key) -> vector
        
        

    };
};